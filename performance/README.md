# Performance Suites

The performance area contains independent parallel and serial performance suites.
Each suite owns its cases, task configuration, launch workflow, processing workflow, cleanup workflow, and generated results.

## Suites

- [Parallel performance](parallel/README.md) measures throughput, workload saturation, and scaling across compute nodes.
- [Serial performance](serial/README.md) will define the serial study workflow.
